<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

    <TextView
        android:id="@+id/texto_dispositivos_vinculados"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="#666"
        android:paddingLeft="5dp"
        android:text="@string/dispositivos_vinculados"
        android:textColor="#fff"
        android:visibility="gone" />

    <ListView
        android:id="@+id/lista_dispositivos_vinculados"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:stackFromBottom="true" >
    </ListView>

    <TextView
        android:id="@+id/texto_nuevos_dispositivos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="#666"
        android:paddingLeft="5dp"
        android:text="@string/otros_dispositivos"
        android:textColor="#fff"
        android:visibility="gone" />

    <ListView
        android:id="@+id/lista_nuevos_dispositivos"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_weight="2"
        android:stackFromBottom="true" >
    </ListView>

    <Button
        android:id="@+id/boton_buscar"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/buscar_dispositivos" />

</LinearLayout>